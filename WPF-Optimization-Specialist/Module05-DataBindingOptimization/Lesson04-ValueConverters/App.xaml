<Application x:Class="ValueConvertersDemo.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ValueConvertersDemo"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Register converters as resources -->
        <local:NaiveFormatConverter x:Key="NaiveFormatter"/>
        <local:CachedFormatConverter x:Key="CachedFormatter"/>
        <local:InstanceCachedFormatConverter x:Key="InstanceCachedFormatter"/>
        <local:NaiveBoolToVisibilityConverter x:Key="NaiveBoolToVisibility"/>
        <local:CachedBoolToVisibilityConverter x:Key="CachedBoolToVisibility"/>
        <local:ExpensiveCalculationConverter x:Key="ExpensiveCalculation"/>
        <local:CachedExpensiveCalculationConverter x:Key="CachedExpensiveCalculation"/>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="140"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
    </Application.Resources>
</Application>
